{
  "name": "cusco-voting-functions",
  "version": "1.0.0",
  "description": "Server-side functions for the CUSCO Voting System (user lifecycle + secure one-vote transaction).",
  "main": "index.js",
  "engines": {
    "node": "22"
  },
  "scripts": {
    "serve": "firebase emulators:start --only functions",
    "deploy": "firebase deploy --only functions"
  },
  "dependencies": {
    "firebase-admin": "^12.1.0",
    "firebase-functions": "^4.9.0"
  },
  "private": true
}