{
  "name": "cusco-voting-system",
  "version": "1.0.0",
  "private": true,
  "description": "CUSCO Voting System MVP — static frontend (Firebase backend wired up separately).",
  "scripts": {
    "start": "node scripts/static-server.js"
  },
  "devDependencies": {
    "@firebase/rules-unit-testing": "^5.0.2"
  }
}
